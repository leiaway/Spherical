import React, { useState } from "react";
import { motion } from "motion/react";
import { Globe, Info, Play, Pause, SkipBack, SkipForward, Shuffle, Repeat } from "lucide-react";
import { Switch } from "../ui/switch";
import { Button } from "../ui/button";
import { Badge } from "../ui/badge";
import { Drawer, DrawerContent, DrawerHeader, DrawerTitle, DrawerTrigger } from "../ui/drawer";
import { currentSong, culturalMetadata } from "../../data/regions";

export function NowPlaying() {
  // ...existing code from Figma, but using project imports and design tokens
}
